## Packages
recharts | For dashboard analytics charts (revenue, order trends)
framer-motion | For smooth transitions and entry animations
date-fns | For formatting dates in tables and details

## Notes
Authentication uses /api/login and /api/user (session based)
Analytics endpoint returns pre-aggregated data
Orders use a status workflow: draft -> confirmed -> packed -> shipped -> delivered
